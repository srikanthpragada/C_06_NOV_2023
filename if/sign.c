// Display sign of the number

#include <stdio.h>

void main()
{
 int num;

  printf("Enter a nmber :");
  scanf("%d",&num);

  if (num >= 0)
    printf("Positive!");
  else
    printf("Negative!");
}
